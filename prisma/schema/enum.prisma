enum ActiveStatus {
  ACTIVE
  BLOCKED
}

enum Role {
  ADMIN
  USER
  AUTHOR
}

enum PostStatus {
  DARFT
  PUBLISHED
  ARCHIVED
}

enum CommentStatus {
  APPROVED
  REJECT
}

enum SubscriptionStatus {
  ACTIVE
  CANCELED
  EXPIRED
}